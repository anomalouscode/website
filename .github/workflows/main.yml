name: ci
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with: 
          path: site
      - uses: aquiladev/ipfs-action@v0.1.5
        with:
          path: ./site
